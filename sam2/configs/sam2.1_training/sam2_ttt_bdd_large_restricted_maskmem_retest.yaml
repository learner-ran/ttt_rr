# @package _global_

defaults:
  - sam2_ttt_bdd_large_restricted_v2
  - _self_

ttt:
  target_mode: maskmem
  anchor_delta_clip: 0.0

launcher:
  experiment_log_dir: ${env:EXP_DIR,./logs/sam_ttt_bdd_restricted_maskmem_retest_run1}

# @package _global_

defaults:
  - sam2_ttt_bdd_large_restricted_no_ttt
  - _self_

trainer:
  model:
    num_maskmem: 0
    use_full_memory: true

launcher:
  experiment_log_dir: ${env:EXP_DIR,./logs/sam_ttt_bdd_baseline_current_only_run1}

# @package _global_

defaults:
  - sam2_ttt_bdd_large_restricted_maskmem_retest
  - _self_

ttt:
  num_layers: 2

launcher:
  experiment_log_dir: ${env:EXP_DIR,./logs/sam_ttt_bdd_restricted_maskmem_l2_run1}
